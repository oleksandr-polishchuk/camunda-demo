# Test configuration using spring-boot

camunda.bpm:
  job-execution.enabled: true
  id-generator: simple

spring.datasource:
  url: jdbc:h2:mem:camunda-spring-boot-test;MODE=MySQL;DB_CLOSE_ON_EXIT=false
spring.h2.console.enabled: true

server:
  port: 8081

